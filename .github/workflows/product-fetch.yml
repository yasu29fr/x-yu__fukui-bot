name: 紹介する商品を足す

# neta/設定.json の「商品の探しかた」にあるキーワードで楽天市場を検索し、
# neta/商品.jsonl を在庫の上限まで補充する。すでにある商品は
# 価格・レビュー・セールの有無を取り直して更新する。
#
# このリストは X と yu の両方が読む（yu は https で読む）。
#
# 投稿文は書かない。書くのは compose.py の仕事。
# ここで入る商品のメモには [未使用] が付くので、
# compose.py は「使った感想」を書かせない。
#
# 毎週月曜 8:00 (JST) = 日曜 23:00 (UTC)。在庫が切れる前に補充する用。
# 手で回したいときは Actions タブ → このワークフロー → Run workflow。
on:
  repository_dispatch:
    types: [product-fetch]
  schedule:
    - cron: "0 23 * * 0"
  workflow_dispatch:
    inputs:
      dry_run:
        description: "ファイルに書かず、見つかった商品を表示するだけ"
        type: boolean
        default: false

permissions:
  contents: write

concurrency:
  group: product-fetch
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: 楽天で探して 商品.jsonl を補充する
        id: fetch
        env:
          RAKUTEN_APP_ID: ${{ secrets.RAKUTEN_APP_ID }}
          RAKUTEN_ACCESS_KEY: ${{ secrets.RAKUTEN_ACCESS_KEY }}
          RAKUTEN_AFFILIATE_ID: ${{ secrets.RAKUTEN_AFFILIATE_ID }}
          # アプリを「Webアプリケーション」で登録したときだけ要る（未登録なら空で動く）
          RAKUTEN_REFERER: ${{ secrets.RAKUTEN_REFERER }}
          DRY_RUN: ${{ inputs.dry_run && '1' || '' }}
        run: |
          set -o pipefail
          node "scripts/商品を取る.mjs" | tee -a "$GITHUB_STEP_SUMMARY"

      - name: コミットする
        if: ${{ !inputs.dry_run }}
        run: |
          set -euo pipefail
          TARGET="neta/商品.jsonl neta/商品_はずした.jsonl"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          # 新しく作られたファイルは git diff に出ないので、先に add してから見る
          git add -- ${TARGET} 2>/dev/null || git add "neta/商品.jsonl"
          if git diff --cached --quiet -- ${TARGET}; then
            echo "${TARGET} に変更なし。コミットはしません。"
            exit 0
          fi
          git commit -m "chore(商品): リストを補充・更新（$(TZ=Asia/Tokyo date +%F)）"
          for attempt in 1 2 3 4; do
            if git pull --rebase origin "${GITHUB_REF_NAME}" && git push; then
              exit 0
            fi
            wait=$((2 ** attempt))
            echo "push に失敗。${wait} 秒待って再試行します。"
            sleep "${wait}"
          done
          exit 1
