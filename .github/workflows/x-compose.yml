name: X 翌日ぶんを作成

# 運用ボードとネタ帳を材料に、翌日ぶんの投稿 3 本を作ってキューに追加する。
# 外部 cron から毎日 20:00 (JST) に x-compose で起動する想定。

on:
  repository_dispatch:
    types: [x-compose]
  workflow_dispatch:
    inputs:
      dry_run:
        description: "true なら生成結果を表示するだけでキューに書き込まない"
        required: false
        default: "false"
      target_date:
        description: "作成対象日 YYYY-MM-DD（空なら翌日）"
        required: false
        default: ""

concurrency:
  group: x-post
  cancel-in-progress: false

jobs:
  compose:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      PYTHONPATH: src
      DRY_RUN: ${{ github.event.inputs.dry_run }}
      # 外部 cron からは client_payload.target_date で当日ぶんの作り直しができる
      TARGET_DATE: ${{ github.event.client_payload.target_date || github.event.inputs.target_date }}
      # ONLY_HOURS=15 のように渡すと、その枠だけを作る
      ONLY_HOURS: ${{ github.event.client_payload.only_hours }}
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
      # ドキュメント ID は秘密ではないので Variables に置く
      BOARD_DOC_ID: ${{ vars.BOARD_DOC_ID }}
      NETA_DOC_ID: ${{ vars.NETA_DOC_ID }}
      ANTHROPIC_MODEL: ${{ vars.ANTHROPIC_MODEL }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 翌日ぶんの 3 本を作る
        run: python scripts/compose.py

      - name: キューの形式を検証する
        run: python -m x_bot validate

      - name: コミットする
        if: ${{ github.event.inputs.dry_run != 'true' }}
        run: |
          if git diff --quiet -- posts/queue.jsonl; then
            echo "キューに変更はありません。"
            exit 0
          fi
          git config user.name "X Compose"
          git config user.email "actions@github.com"
          git add posts/queue.jsonl
          git commit -m "chore(queue): 翌日ぶんを追加"
          git push
